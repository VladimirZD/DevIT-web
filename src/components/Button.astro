---
const { variant = 'solid' } = Astro.props;
---
<button
  class={
    variant === 'solid'
      ? 'inline-flex items-center gap-2 px-4 py-2 rounded-lg bg-accent text-white shadow-sm hover:brightness-95 transition'
      : 'inline-flex items-center gap-2 px-4 py-2 rounded-lg border border-white/10 text-white hover:bg-white/5 transition'
  }
>
  <slot />
</button>
